<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinChatApp.Views.LoginPage"
             xmlns:local="clr-namespace:XamarinChatApp"
             xmlns:vm="clr-namespace:XamarinChatApp.ViewModels;assembly=XamarinChatApp">
    <ContentPage.BindingContext>
        <vm:LoginViewModel/>
    </ContentPage.BindingContext>
    
    <StackLayout Spacing="20" Padding="20" VerticalOptions="Center">
        <Entry Placeholder="Username" Text="{Binding Username}"/>
        <Entry Placeholder="Password" Text="{Binding Password}" IsPassword="true"/>
        <Button Text="Connexion" TextColor="White" BackgroundColor="#2ECC71" Command="{Binding LoginCommand}"/>
        <Button Text="Inscription" TextColor="White" BackgroundColor="#52B3D9" Command="{Binding LoginCommand}"/>
    </StackLayout>

</ContentPage>